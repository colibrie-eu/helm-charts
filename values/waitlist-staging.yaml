image:
  tag: 0.1.3
ingress:
  enabled: true
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/proxy-body-size: 8m
  className: nginx
  hosts:
    - host: waitlist-staging.colibrie.eu
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: waitlist-staging.colibrie.eu-tls
      hosts:
        - waitlist-staging.colibrie.eu
